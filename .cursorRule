---
description: 核心项目规则与约定
alwaysApply: true
---

# 项目通用约定

- 使用 **Java 17**，构建工具为 **Maven（Spring Boot 3）**。
- 所有新代码优先放在 `com.example.ruiji` 包结构下，保持分层清晰（controller / service / mapper / pojo / common / config）。
- 代码、注释与提交信息建议使用 **简体中文**，便于团队统一。

## 编码规范

- 优先使用 **Lombok**（例如 `@Data`、`@Builder`）简化实体类样板代码，但避免在复杂业务类中过度使用以免影响可读性。
- 控制层命名以 `*Controller` 结尾，服务层以 `*Service` / `*ServiceImpl` 命名，MyBatis-Plus 映射以 `*Mapper` 命名。
- 统一通过 `Res`（或类似包装类）封装接口返回值，避免直接返回实体或 `Map`。

## MyBatis-Plus 使用约定

- 开启并遵守逻辑删除字段约定（例如 `deleted` 字段），查询时优先使用框架提供的查询封装，避免手写 SQL 忽略逻辑删除条件。
- 新增表实体时，保持与数据库字段命名一致的驼峰命名，并在必要时使用注解（如 `@TableName`、`@TableField`）保证映射清晰。

## 前端静态资源约定

- 前台页面放在 `src/main/resources/front`，后台管理页面放在 `src/main/resources/backend`，保持目录结构与角色/模块一致。
- 修改静态资源时注意路径相对关系，避免硬编码绝对路径（如 `http://localhost:8080`），优先使用相对路径或由后端统一配置。

## 提交与分支

- 提交信息建议简洁明了，例如：`feat: 新增登录模块`、`fix: 修复订单列表分页问题`。
- 同一提交尽量聚焦一个功能或问题，避免将不相关的大量改动混在一起。
